import { readFile, writeFile, copyFile } from 'node:fs/promises';
const manifest = JSON.parse(await readFile('probe/diagnostic/manifest.json', 'utf8'));
const snippets = JSON.parse(await readFile('probe/vendor/default-snippets.json', 'utf8'));
const esc = value => String(value).replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;');
const usage = snippet => snippet.options.includes('t') || snippet.options.includes('T') ? 'Markdown / TeX: type $$ in Paper' : snippet.options.includes('M') ? 'Use the single-line variant' : snippet.options.includes('v') ? 'Select text, then press the key' : snippet.options.includes('A') ? 'Expands automatically' : 'Type the shortcut, then press Tab';
const rows = snippets.map(snippet => `<tr><td>${snippet.id + 1}</td><td><code>${esc(snippet.regex ? `/${snippet.trigger}/` : snippet.trigger)}</code></td><td><code>${esc(snippet.replacement ?? snippet.dynamic)}</code></td><td>${esc(usage(snippet))}</td></tr>`).join('\n');
const page = `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Keyboard Shortcuts · LaTeX</title><style>body{font:15px/1.6 system-ui;max-width:1100px;margin:36px auto;padding:0 22px;color:#171719;background:#fff}table{border-collapse:collapse;width:100%;margin-top:30px;font-size:13px}th,td{border-bottom:1px solid #e6e6e8;padding:13px 11px;text-align:left;vertical-align:top}code{white-space:pre-wrap;overflow-wrap:anywhere;font:12px ui-monospace,monospace}tr:nth-child(even){background:#fafafa}th{background:#19191c;color:#fff;font-weight:550}a{color:#111;text-underline-offset:4px}h1{font-size:32px;letter-spacing:-1px;font-weight:620}h2{margin-top:26px;font-size:18px}p{max-width:850px;color:#57575b}.note{padding:12px 14px;border-left:3px solid #22bfb2;background:#f6f7f7}</style><h1>Keyboard Shortcuts</h1><p>Version ${manifest.version} · Dropbox Paper for Firefox</p><h2>Using shortcuts</h2><p>Open a Paper equation with <code>$$</code>. Type a shortcut; press Tab when needed to expand it. Use Tab and Shift+Tab to move between fields. Default values are selected so you can replace them as you type.</p><p>Examples: <code>xsr</code>, <code>sq</code>, <code>@a</code>, <code>RR</code>, <code>x/</code>, <code>par</code> then Tab, <code>dint</code>, <code>iden3</code>.</p><p>In matrices, Tab adds a cell (<code>&amp;</code>), Enter adds a row (<code>\\</code>), and Shift+Enter exits the environment. Select an expression and press U, O, B, C, K, S, or a delimiter to wrap it.</p><p class="note"><strong>Experimental:</strong> In regular text, <code>mk</code> and <code>dm</code> can open Paper's native equation entry. Their behavior depends on the Paper editor version.</p><p>Paper decides which commands it can render. Some commands need LaTeX packages Paper may not provide. Dynamic shortcuts run locally; the extension does not download or execute remote code.</p><p><a href="https://github.com/artisticat1/obsidian-latex-suite/tree/265579790a1456c4a7a4a1b893a529041a81c27f">Shortcut source: Obsidian LaTeX Suite</a> · <a href="obsidian-latex-suite-LICENSE.md">MIT license and attribution</a></p><table><thead><tr><th>#</th><th>Trigger / pattern</th><th>Template / function</th><th>How to use</th></tr></thead><tbody>${rows}</tbody></table></html>`;
await writeFile('dist/diagnostic/shortcuts.html', page);
await copyFile('probe/vendor/obsidian-latex-suite-LICENSE.md', 'dist/diagnostic/obsidian-latex-suite-LICENSE.md');
console.log(`Shortcut reference built (${snippets.length} source definitions).`);
