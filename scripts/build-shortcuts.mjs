import {readFile,writeFile,copyFile} from 'node:fs/promises';
const manifest=JSON.parse(await readFile('probe/diagnostic/manifest.json','utf8'));
const snippets=JSON.parse(await readFile('probe/vendor/default-snippets.json','utf8'));
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const state=s=>s.options.includes('t') || s.options.includes('T') ? 'Obsidian/Markdown: usa $$ en Paper' : s.options.includes('M') ? 'Usa su variante de una línea (mismo atajo)' : s.options.includes('v') ? 'Selecciona texto y pulsa la tecla' : s.options.includes('A') ? 'Automático' : 'Escribe y pulsa Tab';
const rows=snippets.map(s=>`<tr><td>${s.id+1}</td><td><code>${esc(s.regex?'/'+s.trigger+'/':s.trigger)}</code></td><td><code>${esc(s.replacement ?? s.dynamic)}</code></td><td>${esc(state(s))}</td></tr>`).join('\n');
await writeFile('dist/diagnostic/shortcuts.html',`<!doctype html><html lang="es"><meta charset="utf-8"><title>Atajos de LaTeX en Paper</title><style>body{font:16px system-ui;max-width:1200px;margin:40px auto;padding:0 20px;color:#111}table{border-collapse:collapse;width:100%}th,td{border-bottom:1px solid #e5e5e5;padding:16px 12px;text-align:left;vertical-align:top}code{white-space:pre-wrap;overflow-wrap:anywhere}tr:nth-child(even){background:#fafafa}th{background:#111;color:#fff;font-weight:500}a{color:#111;text-underline-offset:4px}h1{font-size:36px;letter-spacing:-1.5px;font-weight:600}p{color:#666;line-height:1.7}table{font-size:13px;margin-top:40px}code{font-size:12px}</style><h1>Atajos de LaTeX en Paper · ${manifest.version}</h1><p>Entra en una ecuación con <code>$$</code>. Los atajos se activan automáticamente. Tab avanza entre campos; Shift+Tab vuelve. Los valores iniciales se seleccionan para reemplazarlos al escribir.</p><p>Ejemplos: <code>xsr</code>, <code>sq</code>, <code>@a</code>, <code>RR</code>, <code>x/</code>, <code>par</code> + Tab, <code>dint</code>, <code>iden3</code>.</p><p>En matrices: Tab añade una celda (&amp;), Enter una fila (\\\\), Shift+Enter sale del entorno. Selecciona una expresión y pulsa U, O, B, C, K, S o un delimitador para envolverla.</p><p>Catálogo original: 199 definiciones, incluidos duplicados de modo. Las variantes multilínea se adaptan al campo de una línea de Paper; se conservan los separadores de filas LaTeX. Los atajos Markdown mk/dm se sustituyen por la entrada nativa $$.</p><p>Paper decide qué comandos puede renderizar. Química, bra/ket y otros comandos que requieren paquetes pueden no renderizarse aunque se inserten. Esta versión no modifica el renderizador ni añade concealment o colores al campo.</p><p>En las plantillas, $0, $1… indican campos, \${0:valor} indica un valor inicial y [[0]] una captura del patrón. Las funciones dinámicas del original están adaptadas como código local incluido; no se ejecuta código remoto.</p><p><a href="https://github.com/artisticat1/obsidian-latex-suite/tree/265579790a1456c4a7a4a1b893a529041a81c27f">Fuente: Obsidian LaTeX Suite</a> · <a href="obsidian-latex-suite-LICENSE.md">Licencia MIT y atribución</a></p><table><thead><tr><th>#</th><th>Atajo / patrón</th><th>Plantilla original / función</th><th>Uso en Paper</th></tr></thead><tbody>${rows}</tbody></table></html>`);
await copyFile('probe/vendor/obsidian-latex-suite-LICENSE.md','dist/diagnostic/obsidian-latex-suite-LICENSE.md');
console.log('Shortcut reference built (199 source definitions).');
